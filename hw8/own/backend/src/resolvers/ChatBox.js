const ChatBox = {
  // messages(parent, args, { db }, info) {
  //   let messagelist
  //   for (let i=0; i<parent.messages.length; i++) {
  //     console.log(parent.messages[i].sender.name)
  //   }
  //   return parent.messages.map((message) =>
  //       {  message.body })
  // }
};

export { ChatBox as default };
